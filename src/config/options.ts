import { OptionsIns } from '../types/index';

const defaultOptions: OptionsIns = {
  listenLabel: ['h1', 'h2', 'h3'],
  activeClass: 'toc-active',
  scrollElement: 'html',
};

export default defaultOptions;
